// ─── GitHub DB Config (placeholder for local dev) ───
// On GitHub Pages, this file is replaced by the build workflow
// with the actual PAT token from GitHub Secrets.
// In local dev, data falls back to seed-data.json (read-only).
window.__GITHUB_CRM_CONFIG__ = {
  owner: 'ramkumar4111999-gif',
  repo: 'i-star-opticals-crm',
  pat: '',
  branch: 'main'
};